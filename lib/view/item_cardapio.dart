import 'package:flutter/material.dart';

class ItemCardapioPage extends StatelessWidget {
  const ItemCardapioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}